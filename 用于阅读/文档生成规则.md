# 文档生成规则

从现在开始，项目里的所有文档都要生成两份：

1. 英文工作文档：放在 `docs/` 目录。
2. 中文阅读版：放在 `C:\work\Vibe2\用于阅读` 目录。

`C:\work\Vibe2\用于阅读` 这个文件夹只给用户阅读。Codex 可以往里面写中文文档，但默认不读取这个文件夹，除非用户明确要求。

当英文文档有更新时，应尽量在同一轮同步更新对应的中文阅读版。

